import { ReportingClient } from '@/components/ReportingClient';

export const metadata = {
    title: 'Reporting Instructions - TrustDial',
    description: 'Learn how to find your contact rates and call statistics in every major VOIP dashboard.',
};

export default function ReportingPage() {
    return <ReportingClient />;
}
